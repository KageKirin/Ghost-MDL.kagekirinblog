<style>
{{#foreach posts}}
.demo-blog .post-{{id}}-{{slug}} .mdl-card__media {
	{{#if image}}
		background-image: url('{{image}}');
	{{else}}
		{{! background-image: url('{{asset "images/coffee.jpg"}}'); }}
	{{! TODO set depending on tags}}
	{{/if}}
}

{{> css-author}}

{{/foreach}}
</style>
