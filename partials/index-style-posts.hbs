<style>
body::before {
    background-image: url('{{@blog.cover}}');
}

{{#foreach posts}}
.demo-blog .{{slug}} .mdl-card__media {
  {{#if image}}
    background-image: url('{{image}}');
  {{else}}
    background-image: url('{{asset "images/coffee.jpg"}}');
    {{! TODO set depending on tags}}
  {{/if}}
}
{{/foreach}}

</style>
