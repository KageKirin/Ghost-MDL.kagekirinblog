<nav class="mdl-navigation">
	{{#foreach navigation}}
		<a class="mdl-navigation__link" href="{{url absolute="true"}}">
			{{!-- <i class="material-icons">{{slug}}</i> --}}
			{{label}}
		</a>
	{{/foreach}}
</nav>

{{!--
<a href="{{url absolute="true"}}">
	<button class="mdl-mini-footer--social-btn social-btn__share"></button>
</a>

<li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
--}}
