{{!< common}}

		<main class="mdl-layout__content">
			<div class="demo-blog__posts mdl-grid">
				{{> index-loop-posts}}
				{{pagination}}
			</div>
			{{> common-tags}}
			{{> common-footer}}
		</main>
